import { startRouter as e } from "./router.js";
!(function t() {
  let r = document.getElementById("ano");
  r && (r.textContent = new Date().getFullYear());
  let l = document.querySelector(".site-header"),
    a = document.querySelector(".hamburger"),
    n = document.querySelector(".nav");
  a &&
    n &&
    a.addEventListener("click", () => {
      let e = "true" === a.getAttribute("aria-expanded");
      a.setAttribute("aria-expanded", String(!e)), n.classList.toggle("active");
    }),
    window.addEventListener("scroll", () => {
      l?.classList.toggle("scrolled", window.scrollY > 8);
    }),
    e();
})();
